FROM node:20-alpine

WORKDIR /app

# Install json-server globally to avoid runtime downloads
RUN npm install -g json-server@0.17.4

WORKDIR /data

CMD ["json-server", "--watch", "/data/departments-db.json", "--host", "0.0.0.0", "--port", "3001"]
